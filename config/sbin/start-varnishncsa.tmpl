#!/usr/bin/env bash

# ---------------------------------------------------------------------
# This script is autogenerated by the container startup script (gomplate)
# Purpose: wrap varnishncsa to ensure it uses the correct configuration
# ---------------------------------------------------------------------

set -euo pipefail

exec /usr/bin/varnishncsa -F "{{ .Env.VARNISH_NCSA_LOG_FORMAT }}" \
	-n /app/var/cache/varnish/varnishd \
	-P /app/var/run/varnish/varnishncsa.pid
